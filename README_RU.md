# SillyTavern-Interactive Map Extension

## Описание
**Версия:** 1.0 Beta  
**Автор:** Павел Орлов  
**Адаптация:** СиллиТаверн 1.13.5+  
**Статус:** предварительная версия  
**Лицензия:** CC BY-NC-SA 4.0 http://creativecommons.org/licenses/by/4.0/

Создано с любовью для сообщества SillyTavern (и для меня самого).

Отдельное спасибо:
- Кевину Маклеоду (Kevin MacLeod) (incompetech.com). Его музыка, любезно переданная им в дар человечеству, используется в расширении.
- Команде SillyTavern за потрясающую платформу
- Волонтёрам-тестировщикам

Расширение для SillyTavern, которое добавляет поддержку интерактивных карт с кликабельными зонами, звуковым сопровождением, отображением изображений и видео.

Для наглядности в качестве изображений карт используются мои рисунки по мотивам игры SIMS 4, а в качестве персонажа в последней локации — прекрасная Seraphina.

Возможен абсолютный простор для творчества — от интерактивных карт галактик и звездолётов до карт из любимых игр.

⚠️ **Важное уведомление об авторских правах**

Этот плагин соответствует лицензионному соглашению CC Attribution — Noncommercial — Share Alike (сокращённо CC BY-NC-SA 4.0), автором оригинала является Павел Евгеньевич Орлов.

📜 **Условия использования:**

Эта лицензия позволяет другим перерабатывать, исправлять и развивать произведение на некоммерческой основе, при условии, что они упоминают оригинальное авторство и лицензируют 
производные работы на аналогичных лицензионных условиях. Пользователи могут не только получать и распространять произведение на условиях, идентичных данной лицензии («by-nc-sa»), 
но и переводить, создавать иные производные работы, основанные на этом произведении. Все новые произведения, основанные на этом, будут иметь одни и те же лицензии, 
поэтому все производные работы также будут носить некоммерческий характер. 

Удаление или фальсификация информации об авторе, либо коммерческое использование нарушает лицензионное соглашение CC BY-NC-SA 4.0, 
за что на нарушителя возлагается юридическая ответственность в соответствии с тяжестью совершённого правонарушения. 
У этого проекта есть полная история отправки на GitHub для проверки оригинальности.

## Основные возможности

- 🗺️ Загрузка и отображение интерактивных SVG-карт с фоновыми изображениями
- 🖱️ Кликабельные зоны с выполнением STScript команд — одной или сразу нескольких (персонаж в локации, вступительная реплика, звук, изображение, видео)
- 🔈 Звуковое сопровождение карт (MP3, OGG, WAV, M4A, WebM)
- 🖼️ Отображение изображений в окне карты (PNG, JPG, JPEG, WebP, GIF)
- 🎬 Воспроизведение видео в окне карты (MP4, WebM, OGG, M4V)
- 📂 Автоматическое обнаружение карт через `index.json`
- 🔄 Поддержка множества карт с динамическим выбором и возможностью их вкладывания одна в другую в виде иерархии
- 🎨 Визуальные эффекты при наведении курсора на интерактивные зоны
- 💾 Кэширование загруженных карт для повышения производительности

## Структура файлов

```
scripts/extensions/third-party/SillyTavern-Interactive Map/
├── index.js          # Основной файл расширения
├── index.json        # Индекс доступных карт (опционально)
├── maps/             # Папка с JSON-файлами карт
│   └── example.json
├── images/           # Фоновые изображения карт
│   └── example.png
├── sounds/           # Звуковые файлы
│   └── ambient.mp3
├── movies/           # Видеофайлы
│   └── cutscene.mp4
└── README.md         # Документация
```

## Формат файла карты

Каждая карта представляет собой изображение .png и файл JSON с тем же именем, который определяет зоны, отмеченные на изображении, и связанные с ними скрипты со следующей структурой (пример):

```json
{
	"version": "1.0 Beta",
	"mapSound": "Test.mp3",
	"metadata": {
		"name": "Chess-table",
		"author": "Pavel Orlov",
		"description": "Интерактивная карта с шахматным столиком у фонтана",
		"created": "2025-11-01",
		"updated": "2025-11-01"
	},
	"backgroundImage": {
		"file": "images/background.png",
		"width": "1920",
		"height": "1080"
	},
	"mapSound": "sounds/ambient.mp3",
	"shapes": [
		{
			"id": "zone1",
			"type": "object",
			"path": "M 100, 100 L 200, 100 L 200, 200 L 100, 200 Z",
			"color": "#FF0000",
			"tooltip": "Кликните для действия",
			"script": "/..."
		}
	]
}
```

### Поля карты

#### backgroundImage (обязательное)
- `file` (string): Путь к изображению карты относительно папки расширения
- `width` (number/string): Ширина изображения в пикселях
- `height` (number/string): Высота изображения в пикселях

#### mapSound (опциональное)
- Путь к аудиофайлу для автоматического воспроизведения при загрузке карты

#### shapes (обязательное) — объекты карты, выделенные SVG-зонами

Массив интерактивных зон:
- `id` (string): Уникальный идентификатор зоны
- `type` (string): Тип объекта
- `path` (string): SVG path для определения области зоны в строчной записи
- `color` (string): Цвет заливки при наведении (формат HEX: #RRGGBB или #RGB)
- `script` (string): STScript команда или команды для выполнения при клике на SVG-зону карты
- `tooltip` (string, опциональное): Текст всплывающей подсказки

## Файл index.json

Для автоматического обнаружения карт создайте файл `index.json` в корне расширения:

```json
[
	"maps/city.json",
	"maps/dungeon.json",
	"maps/world.json"
]
```

Если файл не найден, расширение будет использовать карту по умолчанию: `SillyTavern.json`.

## Slash-команды

### /showmap [имя_карты]
Открывает окно карты и загружает указанную карту.

**Примеры:**
```
/showmap city
/showmap maps/dungeon.json
/showmap
```

- Без параметров загружает последнюю выбранную карту
- Поддерживает частичное совпадение имён файлов
- Автоматически добавляет расширение `.json` если не указано

### /showmap_sound [имя_звука] [sound=другой_файл]
Воспроизводит звук из папки `sounds/`.

**Примеры:**
```
/showmap_sound ambient
/showmap_sound sounds/forest.mp3
/showmap_sound "Secluded corner"
```

**Параметры:**
- Основной аргумент: имя файла (расширение опционально, по умолчанию `.mp3`)
- `sound=`: альтернативное имя файла для воспроизведения

Аналогично для файлов видео и изображений.

### /stopsound
Останавливает текущее воспроизведение звука карты.

**Пример:**
```
/stopsound
```

### /showmap_image [файл] [size=NN]
Отображает изображение в окне карты из папки `images/`.

**Примеры:**
```
/showmap_image portrait.png
/showmap_image scene size=80
/showmap_image images/artwork.jpg size=50
```

**Параметры:**
- Основной аргумент: имя файла (расширение опционально, по умолчанию `.png`)
- `size=NN`: размер в процентах (10-100)

**Поддерживаемые форматы:** PNG, JPG, JPEG, WebP, GIF

### /stopimage
Скрывает текущее изображение в окне карты.

**Пример:**
```
/stopimage
```

### /showmap_video [файл] [muted=0/1] [loop=0/1] [size=NN]
Воспроизводит видео в окне карты из папки `movies/`.

**Примеры:**
```
/showmap_video cutscene.mp4
/showmap_video intro muted=1 loop=1
/showmap_video battle size=60
/showmap_video movies/outro.webm muted=0 loop=0 size=80
```

**Параметры:**
- Основной аргумент: имя файла (расширение опционально, по умолчанию `.mp4`)
- `muted=1`: отключить звук (0 или 1)
- `loop=1`: зациклить воспроизведение (0 или 1)
- `size=NN`: размер в процентах (10-100)
- По умолчанию видео воспроизводится автоматически

**Поддерживаемые форматы:** MP4, WebM, OGG, M4V

### /stopvideo
Останавливает и скрывает текущее видео в окне карты.

**Пример:**
```
/stopvideo
```

## Использование в UI

### Кнопка в меню расширений (внизу)
После установки в меню расширений внизу появится кнопка **"🌍 Последняя выбранная карта"**, которая открывает последнюю выбранную карту.

### Кнопки в панели настроек расширений (вверху)
В разделе **Extensions Settings** доступны:
- Выпадающий список для выбора карты
- Кнопка **"Загрузить карту"**
- Кнопка **"Обновить"** для обновления списка доступных карт

## Настройки расширения

Настройки задаются в объекте `mapSettings` в файле `index.js`:

```javascript
const mapSettings = {
	hoverOpacity: 0.3,           // Прозрачность зон при наведении (0-1)
	transitionDuration: 200,     // Длительность анимации в мс
	enableTooltips: true,        // Включить всплывающие подсказки
	debugMode: false,            // Режим отладки в консоли
	maxMapCache: 10,             // Максимум карт в кэше
	fetchTimeout: 10000,         // Таймаут загрузки карты (мс)
	indexTimeout: 3000,          // Таймаут загрузки index.json (мс)
	defaultMap: 'SillyTavern.json' // Карта по умолчанию
};
```

## Создание SVG path для зон

При создании определений границ SVG зон я использовал [Image Coordinate Picking Online](https://www.lddgo.net/en/image/coordinate-pick).

Для преобразования данных в строчный формат и их копирования в JSON карты я написал программу `SVG_points_→_M-L-Z.html` (находится в директории Tools).

SVG path можно создать несколькими способами:

### 1. Графические редакторы
- **Inkscape** (бесплатный): нарисуйте фигуру → скопируйте атрибут `d` из XML
- **Adobe Illustrator**: экспортируйте как SVG → скопируйте path
- **Figma/Sketch**: экспортируйте SVG → извлеките path

### 2. Онлайн-инструменты
- [Image Coordinate Picking Online](https://www.lddgo.net/en/image/coordinate-pick)
- [SVG Path Editor](https://yqnn.github.io/svg-path-editor/)
- [Method Draw](https://editor.method.ac/)

### 3. Примеры path

**Прямоугольник:**
```
M 10,10 L 100,10 L 100,100 L 10,100 Z
```

**Круг (приблизительно):**
```
M 50,10 A 40,40 0 1,1 50,90 A 40,40 0 1,1 50,10 Z
```

**Многоугольник:**
```
M 50,10 L 90,90 L 10,50 Z
```

## Безопасность

Расширение включает защиту от path traversal атак:
- Запрещены пути с `..`
- Запрещены абсолютные пути
- Все пути должны быть относительными внутри папки расширения

## Требования

- SillyTavern (последняя версия)
- jQuery (встроен в SillyTavern)
- Современный браузер с поддержкой ES6+

---

**Версия документации:** 1.0  
**Дата:** Декабрь 2025
